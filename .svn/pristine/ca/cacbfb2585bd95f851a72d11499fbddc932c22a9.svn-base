package com.zplh.zplh_android_yk;

import org.junit.Test;

import static org.junit.Assert.assertEquals;

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * @see <a href="http://d.android.com/tools/testing">Testing documentation</a>
 */
public class ExampleUnitTest {

    @Test
    public void addition_isCorrect() throws Exception {

//        str=str.Substring(str.Length-i); // or str=str.Remove(0,str.Length-i);

        String ss="A00fdsfs0101";
//        str=str.Substring(0,str.Length-i); // or str=str.Remove(str.Length-i,i);
        System.out.println(ss.substring(0,ss.length()-4));
        System.out.println(ss.substring(ss.length()-4));


   /*     String regEx = "[^0-9]";
        Pattern p = Pattern.compile(regEx);
        Matcher m = p.matcher(ss.substring(ss.length()-7));
        int b = Integer.parseInt(m.replaceAll("").trim());//群人数
        System.out.println(b);*/
        assertEquals(4, 2 + 2);
    }




}